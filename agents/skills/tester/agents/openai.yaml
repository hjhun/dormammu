interface:
  display_name: "Tester Workflows"
  short_description: "Runs authored tests and CLI scenarios, then reports pass/fail evidence."
  default_prompt: "Validate the active Dormammu work through tests and CLI scenarios, then end with the required OVERALL verdict."
