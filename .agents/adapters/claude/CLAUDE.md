# Claude Adapter

Claude should use the Dormammu guidance bundle in `.agents/`.

Load `.agents/AGENTS.md` first, then load the active role skill from
`.agents/roles/<role>/SKILL.md`.

For normal development, follow
`.agents/workflows/autonomous-development-loop.md`. For simple tasks, follow
`.agents/workflows/simple-task.md`.

