# agy(Antigravity) Adapter

agy should use `.agents/AGENTS.md` as the top-level instruction file.

The active role determines which skill to load from `.agents/roles/`.
The default workflow is `.agents/workflows/autonomous-development-loop.md`.
Use `.agents/workflows/recovery-loop.md` when resuming failed or interrupted
work.

