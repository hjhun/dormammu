# Cline Adapter

Cline should use the shared Dormammu role bundle in `.agents/`.

Start with `.agents/AGENTS.md`, then load the role-specific skill document
for the current stage. Keep `.dev` state files aligned with the active role.

Use unit, smoke, and e2e validation gates according to the reviewer skill.

